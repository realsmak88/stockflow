% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lbb.R
\name{run_lbb_froese}
\alias{run_lbb_froese}
\title{Running Froese's LBB script (R2jags backend)}
\usage{
run_lbb_froese(
  data,
  stock = NULL,
  workdir = file.path(tempdir(), "LBB_run"),
  script = NULL,
  ...
)
}
\arguments{
\item{data}{data.frame \code{prepare_lbb_data()} (Stock, Year, Length,
CatchNo).}

\item{stock}{Stock identifier.}

\item{workdir}{Working directory (outputs, including ggplot2 figures,
are written there under \code{LBB_Results/}).}

\item{script}{Path to the LBB script. Defaults to the one provided by
the package.}

\item{...}{Priors passed to \code{write_lbb_inputs()} / \code{write_lbb_id()}
(mm, MK_prior, Linf_prior, Lm50, ...).}
}
\value{
S3 object \code{FishStockLBB} (\code{fit} = raw Ldat data.frame,
  \code{summary} = harmonized indicators, \code{ref_levels},
  \code{output_dir}).
}
\description{
Prepares the input files, runs the official
\code{LBB_ggplot.R} script (provided in \code{inst/scripts/}) without
modifying its scientific core, then retrieves the annual results table.
}
\section{Prerequisites}{

  Packages \code{R2jags}, \code{Hmisc}, \code{ggplot2}, \code{crayon} and
  the \strong{JAGS} software must be installed.
}

\examples{
\dontrun{
  dd  <- prepare_lbb_data(freq, "LCT", "annee", "n", stock = "Penaeus")
  fit <- run_lbb_froese(dd, mm = TRUE, MK_prior = 1.5, Linf_prior = 45)
  fit$summary
}
}
\references{
Froese, R. et al. (2018). ICES J. Mar. Sci. 75(6): 2004-2015.
}
