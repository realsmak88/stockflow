% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/selectivity.R
\name{fit_gillnet_selectivity}
\alias{fit_gillnet_selectivity}
\title{Fit the selectivity of a gillnet (Millar, TropFishR)}
\usage{
fit_gillnet_selectivity(
  data,
  x0 = NULL,
  rtype = "norm.loc",
  rel.power = NULL,
  plot = FALSE
)
}
\arguments{
\item{data}{List in the format expected by \code{select_Millar}:
\code{midLengths} (size classes), \code{meshSizes} (mesh sizes) and
\code{CatchPerNet_mat} (catch-per-net matrix).}

\item{x0}{Initial values of the model (see \code{?select_Millar}).}

\item{rtype}{Type of retention curve: \code{"norm.loc"} (default),
\code{"norm.sca"}, \code{"lognorm"}, \code{"binorm.sca"}, \code{"gamma"}.}

\item{rel.power}{Relative fishing power per mesh size (optional).}

\item{plot}{Logical: plot the \code{select_Millar} diagnostic.}
}
\value{
Object of class \code{FishStockSelectivity}: list containing the
  raw output of \code{select_Millar()} (\code{$fit}), the gear type
  (\code{"gillnet"}) and the main estimates (\code{$estimates}).
}
\description{
Wrapper around \code{TropFishR::select_Millar()}. Fits a log-linear
selectivity model to experimental catch-per-mesh data
(size x mesh matrix), and derives the relationship between mesh size and
selected size.
}
\examples{
\dontrun{
data(gillnet, package = "TropFishR")
fit <- fit_gillnet_selectivity(gillnet, x0 = c(60, 4))
}
}
\references{
Millar & Holst (1997); Sparre & Venema (1998).
}
\seealso{
\code{\link{gillnet_mesh_for_Lc}}, \code{\link{retention_curve}}
}
