% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/growth.R
\name{fit_vbgf_age}
\alias{fit_vbgf_age}
\title{von Bertalanffy fit from age-length data (FSA)}
\usage{
fit_vbgf_age(
  data,
  age_col,
  length_col,
  type = "typical",
  boot = FALSE,
  nboot = 200
)
}
\arguments{
\item{data}{data.frame containing age and length.}

\item{age_col, length_col}{Names of the age and length columns.}

\item{type}{von Bertalanffy parameterization (\code{"typical"} by default ;
see \code{FSA::vbFuns()}).}

\item{boot}{Logical. Compute bootstrap CIs.}

\item{nboot}{Number of bootstrap resamples.}
}
\value{
S3 object \code{FishStockVBGF} (compatible with the parameter
  extractors of the mortality module via \code{$par}).
}
\description{
Complement to the ELEFAN methods (length-frequency) : fits the
von Bertalanffy model when individual ages are available, via the
official functions \code{FSA::vbStarts()} and \code{FSA::vbFuns()} and
\code{stats::nls()}. Bootstrap confidence intervals are computed
with \code{FSA::bootCase()} / \code{car::Boot()} if available.
}
\examples{
\dontrun{
  vb <- fit_vbgf_age(agedata, "age", "TL", boot = TRUE)
  vb$par
}
}
\references{
von Bertalanffy, L. (1938). Human Biology 10.
  Ogle, D.H. (2016). Introductory Fisheries Analyses with R.
}
