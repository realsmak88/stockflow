% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/closure.R
\name{optimal_closure}
\alias{optimal_closure}
\title{Determine the optimal window and duration for biological rest}
\usage{
optimal_closure(
  spawning = NULL,
  recruitment = NULL,
  duration = NULL,
  max_duration = 6,
  w_spawning = 0.5,
  w_recruitment = 0.5
)
}
\arguments{
\item{spawning}{\code{data.frame} from \code{spawning_season()}
(\code{prop_mature} column). Optional.}

\item{recruitment}{\code{data.frame} from \code{recruitment_pattern()}
(\code{per_recruits} column). Optional. At least one of the two signals
is required.}

\item{duration}{Closure duration in months (integer 1-11) or \code{NULL} to
compare 1 to \code{max_duration} months.}

\item{max_duration}{Maximum duration evaluated when \code{duration = NULL}
(default 6).}

\item{w_spawning, w_recruitment}{relative weight of the two signals in the score
(normalized internally). Default 0.5 / 0.5.}
}
\value{
Object of class \code{FishStockClosure}: list containing the monthly
  score (\code{$monthly}), the best window for the requested duration
  (\code{$best}) and, where applicable, the duration comparison table
  (\code{$by_duration}).
}
\description{
Combines the monthly spawning and recruitment signals into a closure
score, then identifies the contiguous window of \code{duration} months that
maximizes the cumulative protected score. Since the year is cyclical, the
search is circular (a closure can overlap December-January). If
\code{duration = NULL}, all durations from 1 to 6 months are evaluated and
compared.
}
\examples{
\dontrun{
sp <- spawning_season(bio, "Date", "maturity", mature_codes = c(2,3,4))
rc <- recruitment_pattern(lfq)
optimal_closure(spawning = sp, recruitment = rc, duration = 3)
}
}
\seealso{
\code{\link{spawning_season}}, \code{\link{recruitment_pattern}}
}
