% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots.R
\name{plot_gsi}
\alias{plot_gsi}
\title{Gonadosomatic ratio (GSR / gonadosomatic index)}
\usage{
plot_gsi(
  data,
  gonad_col,
  weight_col,
  date_col = NULL,
  sex_col = NULL,
  sex_filter = NULL,
  max_gsi = 20,
  level = 0.95,
  title = "Gonadosomatic ratio (GSR)"
)
}
\arguments{
\item{data}{data.frame of individual measurements.}

\item{gonad_col}{Gonad weight column (same unit as
\code{weight_col}).}

\item{weight_col}{Somatic / total weight column of the individual.}

\item{date_col}{Date column (for the monthly evolution). If
\code{NULL}, only the overall distribution is plotted.}

\item{sex_col}{Sex column (optional). If provided with
\code{sex_filter}, restricts the computation (e.g. to females).}

\item{sex_filter}{Value(s) of \code{sex_col} to keep (e.g.
\code{"F"}). Ignored if \code{sex_col} is \code{NULL}.}

\item{max_gsi}{Upper bound anti-artifact: GSR values above (in \%)
are discarded (default 20, suited to small pelagics / crustaceans;
increase for species with high gonadal investment).}

\item{level}{Confidence level of the monthly bands (default 0.95).}

\item{title}{Plot title.}
}
\value{
A \code{ggplot} object. The GSR peak month is annotated.
}
\description{
Plots the monthly evolution of the gonadosomatic ratio
\eqn{GSR = 100 \times P_{gonad} / P_{somatic}}, monthly mean
surrounded by its confidence interval. The GSR peak marks the
period of gonadal maturation / spawning and complements the biological
rest analysis (see \code{\link{spawning_season}}). An artifact filter
discards GSR values deemed physiologically improbable.
}
\examples{
\dontrun{
plot_gsi(penaeus_bio, "gonad_weight", "weight", date_col = "Date",
         sex_col = "sex", sex_filter = "F")
}
}
\references{
King (2007), Fisheries Biology; West (1990),
  Aust. J. Mar. Freshwater Res. 41, fecundity estimation methods
  and gonadic indices.
}
\seealso{
\code{\link{spawning_season}}, \code{\link{optimal_closure}}
}
