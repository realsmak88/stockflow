% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/growth.R
\name{best_growth_model}
\alias{best_growth_model}
\title{Multi-criteria selection of the best growth model}
\usage{
best_growth_model(
  comparison,
  criterion = c("Rn_max", "score", "balanced", "fastest", "Linf", "K"),
  verbose = TRUE
)
}
\arguments{
\item{comparison}{\code{data.frame} produced by
\code{\link{compare_growth_models}}.}

\item{criterion}{Selection criterion : \code{"Rn_max"} (default),
\code{"score"}, \code{"balanced"}, \code{"fastest"}, \code{"Linf"} or
\code{"K"}.}

\item{verbose}{Logical. Display the selected model in the console.}
}
\value{
An (invisible) list : \code{best} (row of the selected model),
  \code{ranking} (ordered table) and \code{criterion}.
}
\description{
Ranks the compared models according to the chosen criterion and returns the best one.
The \code{"balanced"} criterion combines the fit score (70 percent) and the
stability of parameters around the mean of the methods (30 percent), so as
not to retain a model with a high score but atypical parameters.
}
\examples{
\dontrun{
  sel <- best_growth_model(cmp, criterion = "balanced")
  sel$best
}
}
\seealso{
\code{\link{compare_growth_models}}, \code{\link{choose_best_growth}}
}
