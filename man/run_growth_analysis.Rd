% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/growth.R
\name{run_growth_analysis}
\alias{run_growth_analysis}
\title{Complete growth analysis (multi-method + automatic selection)}
\usage{
run_growth_analysis(
  lfq,
  methods = c("elefan_ga", "elefan_sa", "elefan"),
  powell = TRUE,
  Linf_range = NULL,
  K_range = NULL,
  criterion = "Rn_max",
  control = list(),
  verbose = TRUE,
  ...
)
}
\arguments{
\item{lfq}{Object \code{lfq}.}

\item{methods}{Subset of
\code{c("elefan", "elefan_ga", "elefan_sa")}.}

\item{powell}{Logical. Include Powell-Wetherall as a diagnostic.}

\item{Linf_range, K_range}{Search ranges passed to the ELEFAN methods
and to the bounds check.}

\item{criterion}{Selection criterion (see \code{best_growth_model}).}

\item{control}{List of control arguments passed to each method.}

\item{verbose}{Display progress and the best model.}

\item{...}{Additional arguments passed to the ELEFAN functions.}
}
\value{
S3 object \code{FishStockGrowthAnalysis} : list \code{models},
  \code{comparison} (data.frame), \code{best}, \code{criterion} and
  \code{powell} (optional).
}
\description{
Runs the requested ELEFAN methods (classic, GA, SA), builds the
multi-criteria comparative table and automatically selects the best
model. Powell-Wetherall can be included as a diagnostic for initial
values (not compared, as it has a different structure).
}
\examples{
\dontrun{
  ga_an <- run_growth_analysis(lfq,
                               Linf_range = c(70, 100),
                               K_range = c(0.1, 0.5),
                               criterion = "Rn_max")
  ga_an$comparison
  ga_an$best
}
}
