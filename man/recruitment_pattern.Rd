% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/closure.R
\name{recruitment_pattern}
\alias{recruitment_pattern}
\title{Monthly recruitment pattern (TropFishR)}
\usage{
recruitment_pattern(lfq, tsample = NULL, ...)
}
\arguments{
\item{lfq}{\code{lfq} object from \pkg{TropFishR} containing at least
\code{midLengths}, \code{catch}, \code{Linf}, \code{K} and \code{t0}.}

\item{tsample}{Sampling time (fraction of the year). If \code{NULL},
inferred from the \code{lfq} dates when available.}

\item{...}{Arguments passed to \code{TropFishR::recruitment()}.}
}
\value{
\code{data.frame} \code{month, month_label, per_recruits}
  (percentage of recruits per month, 12 rows).
}
\description{
Wrapper around \code{TropFishR::recruitment()} that reconstructs, from
an \code{lfq} object (length frequencies + growth parameters), the
percentage of recruits per month. The peak(s) indicate the arrival of
juveniles, a second target for biological rest.
}
\references{
Sparre & Venema (1998), FAO 306/1.
}
