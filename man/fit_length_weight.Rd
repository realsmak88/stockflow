% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/allometry.R
\name{fit_length_weight}
\alias{fit_length_weight}
\title{Length-weight relationship (allometry) W = a * L^b}
\usage{
fit_length_weight(data, length_col, weight_col, group = NULL, log_base = 10)
}
\arguments{
\item{data}{data.frame containing the lengths and weights.}

\item{length_col, weight_col}{Names of the length and weight columns.}

\item{group}{(optional) Name of a group column (sex, species, zone)
to compare relationships.}

\item{log_base}{Logarithmic base (\code{10} by default, fisheries
convention; \code{exp(1)} possible).}
}
\value{
S3 object \code{FishStockLW}: list with \code{a}, \code{b}, the CI of
  \code{b}, the isometry test, the \eqn{r^2}, the sample size, the
  \code{lm} model and, where applicable, the group comparison.
}
\description{
Fits the length-weight relationship by log-log linear regression and tests
isometry (\eqn{b = 3}) via the official function \code{FSA::hoCoef()}.
An inter-group comparison (ANCOVA) is provided when \code{group}
is specified.
}
\examples{
\dontrun{
  lw <- fit_length_weight(bio, "LM", "weight")
  lw$a; lw$b
}
}
\references{
Le Cren, E.D. (1951). J. Anim. Ecol. 20(2).
  Froese, R. (2006). J. Appl. Ichthyol. 22.
  Ogle, D.H. (2016). Introductory Fisheries Analyses with R.
}
